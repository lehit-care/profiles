spring:
  cloud:
    config:
      uri: http://config-server
      failFast: true
      retry:
        initialInterval: 3000
        multiplier: 1.3
        maxInterval: 10000
        maxAttempts: 15
